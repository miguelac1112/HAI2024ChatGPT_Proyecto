import * as avatar from "./avatar.js"
import buttonBehavior from "./buttonBehavior.js";

// Seleccionar el canvas por su ID (o cualquier otro selector)
const canvas = document.getElementById('live2d');

// Modificar el ancho y alto del canvas
canvas.width = 1600;
canvas.height = 700;
